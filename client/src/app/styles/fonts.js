import { Raleway, Mukta } from "next/font/google";

export const mukta = Mukta({
  subsets: ["latin"],
  variable: "--mukta",
  weight: "400",
});

export const raleway = Raleway({
  subsets: ["latin"],
  variable: "--raleway",
  weight: "400",
});
